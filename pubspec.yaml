name: geo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=2.19.4 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
 
  cupertino_icons: ^1.0.2
  geolocator: ^9.0.2
  flutter_map: ^3.1.0
  latlong2: any
  smart_timer: ^1.0.2
  deta: ^0.0.3+1
  http_client_deta_api: ^0.0.1+1
  dio_client_deta_api: ^0.0.1
  dio: ^4.0.1
  flutter_dotenv: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^2.0.0


flutter:

 
  uses-material-design: true

  
  assets:
    - assets/.env
  

 